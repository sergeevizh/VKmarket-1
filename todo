VKMarket :
    [x] Заменить вывод результата с "json" на "array" в md-файле

    Абстракции :
        [x] `request()`     вызов метода
        [x] `upload()`      загрузка файла на сервер ВКонтакте
    
    
VKsync :
    [ ] Переделать реализацию плагина на абстракции
    [ ] Вынести конфигурацию в CMS

    Абстракции :
        [x] `params()`                      генерирует массив параметров для ВК
        [ ] `check($value)`                 проверяет, была ли связь элемента с ВК  => 0 | идентификатор в ВК
        [ ] `differ($before, $after)`       проверяет, есть ли отличия ДО и ПОСЛЕ   => 0 | массив отличающихся параметров

        [ ] `add($case, $params)`           добавление нового элемента в ВК
        [ ] `edit($case, $params)`          редактирование элементв в ВК
        [ ] `delete($case, $params)`        удаление элемента в ВК
        
        [x] `alert()`                       вывод сообщения в лог EVO
        [ ] `sync($tv, $value)`             установка связи элемента с ВК
        [ ] `desync($tv)`                   разрыв связи элемента с ВК