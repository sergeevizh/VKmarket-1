VKapi :

    Абстракции :
        [x] `request()`     вызов метода
        [x] `upload()`      загрузка файла на сервер ВКонтакте
    
    
VKsync :

    [x] настроить связь товаров с подборками
    [ ] настроить размещение элементов (сортировку)

    Абстракции :
        [x] `params()`      генерирует массив параметров для ВК
        [x] `check()`       проверяет, была ли связь элемента с ВК  => 0 | идентификатор в ВК
        [x] `differ()`      проверяет, есть ли отличия ДО и ПОСЛЕ   => 0 | массив отличающихся параметров

        [x] `add()`         добавление нового элемента в ВК
        [x] `edit()`        редактирование элемента в ВК
        [x] `delete()`      удаление элемента из ВК
        
        [x] `alert()`       вывод сообщения в лог EVO